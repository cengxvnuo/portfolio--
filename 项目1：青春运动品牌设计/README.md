# Campus Gravity Break

Interactive Vite + React microsite for an AI-generated campus sports poster system.

## Setup

```bash
npm install
npm run dev
```

Poster placeholders live in `public/posters/`. Object placeholders live in `public/objects/`.

Replace the placeholder poster comments in the React components with your own generated campaign images when ready.
